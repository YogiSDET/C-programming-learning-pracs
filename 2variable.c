# include<stdio.h>

int main()
{
    int a = 4; 
    // // int b=3.4; Not recommend because 8.5 is not a integer

    float b = 8.5;
    char c = 'u';
    int d = 4;
    int e = 34 + 1; 
    printf("the value of a is %d \n", a);
    printf("the value of b is %f \n", b);
    printf("the value of c is %c \n", c);
    printf("Sum of a and d is %d \n", a + d);
    printf("Sum of a and e is %d \n", e);

    return 0;
}