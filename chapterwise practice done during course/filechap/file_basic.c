#include<stdio.h>

int main(){
    //FILE *ptr;
    //ptr = fopen("sample.txt", "r");
    //ptr = fopen("sample.txt" ,"w"); // this write will generate new sample.txt file in 
    // your folder of file so w means write and r --> read


    return 0;
}